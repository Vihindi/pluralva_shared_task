# GPU environment for Stage-1 scripts (bootstrap_rationales.py, train_lora.py, evaluate.py)
# Local preprocessing (preprocess.py, build_sft_data.py) needs only the standard library.
torch>=2.3
transformers>=4.51        # Qwen3 support + enable_thinking chat-template kwarg
peft>=0.11
trl>=0.19                 # SFTConfig(assistant_only_loss=True)
datasets>=2.19
accelerate>=0.30
bitsandbytes>=0.43        # only needed for --load_4bit (QLoRA on 16-24GB GPUs)
